module;
#include <boost/unordered/unordered_flat_map.hpp>

export module A;

export namespace A
{
    template<class T>
    struct X
    {
        boost::unordered::unordered_flat_map< int, T > extensions;
    };
}
